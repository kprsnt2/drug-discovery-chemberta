# Drug Discovery Text Generation Model Requirements
# Optimized for AMD MI300X 192GB VRAM

# Core ML
torch>=2.1.0
transformers>=4.36.0
accelerate>=0.25.0

# Training
trl>=0.7.0
peft>=0.7.0
datasets>=2.16.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Drug data APIs
chembl-webresource-client>=0.10.0
pubchempy>=1.0.4
requests>=2.28.0

# UI
gradio>=4.0.0

# Utilities
tqdm>=4.65.0

# Optional: Quantization (not needed with 192GB VRAM)
# bitsandbytes>=0.42.0  # For 4-bit/8-bit if needed

# Optional: Experiment tracking
# wandb>=0.16.0

# Optional: Flash Attention (for AMD ROCm)
# flash-attn>=2.3.0  # May need special installation for ROCm
